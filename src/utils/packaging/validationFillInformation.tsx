import React, { useState } from "react";
import { TextField, Box, Button } from "@mui/material";

export default function FormValidation(
  headShift: Array<string>,
  supervisor: Array<string>,
  shear: Array<string>,
  productionPlanningStops: Array<string>,
  
) {}
